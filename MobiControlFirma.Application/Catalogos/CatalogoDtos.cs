namespace MobiControlFirma.Application.Catalogos;

/// <summary>Elemento de catálogo (distrito, canal o estado) para poblar selectores.</summary>
public record CatalogoItemDto(int Id, string Nombre);
